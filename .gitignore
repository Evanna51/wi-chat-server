node_modules/
.env

data/*.db
data/*.db-shm
data/*.db-wal
data/*.db.bak*
data/vector-index.bin
data/vector-meta.json
data/archive/
data/backup/
data/backups/
exports/

logs/
.pm2/

# A/B prompt test reports & local scratch
tmp/

npm-debug.log*

# Claude Code local worktrees / agent state — never commit
.claude/
# Playwright MCP session cache（截图 / DOM snapshot / console log）
.playwright-mcp/

# Firebase / GCP service account keys (any name)
*service-account*.json
firebase-adminsdk-*.json
